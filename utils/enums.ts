export enum Timeout {
    SHORT = 3000,
    MEDIUM = 10000,
    LONG = 30000,
}

export enum Index {
    First = 1, 
    Second = 2,
    Third = 3,
    Fourth = 4,
}

export enum Button {
    PREVIOUS = "previous",
    NEXT = "next",
}
